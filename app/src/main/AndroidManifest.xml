<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tareassp1.milaronix.tareassp1" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/link_iclauncher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".midetalle"
            android:label="@string/title_activity_midetalle" >
        </activity>
        <activity
            android:name=".ImgLogo"
            android:label="@string/title_activity_img_logo" >
        </activity>
    </application>

</manifest>
